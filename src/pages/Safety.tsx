import styled from "styled-components";

const Container = {
  Container: styled.main``,
};

const Safety = () => {
  return <Container.Container>test</Container.Container>;
};

export default Safety;
